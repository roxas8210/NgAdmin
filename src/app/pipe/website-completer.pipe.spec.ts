import { WebsiteCompleterPipe } from './website-completer.pipe';

describe('WebsiteCompleterPipe', () => {
  it('create an instance', () => {
    const pipe = new WebsiteCompleterPipe();
    expect(pipe).toBeTruthy();
  });
});
